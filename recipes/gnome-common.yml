# GNOME Configuration and Tweaking
modules:

  # Installs fonts
  - type: fonts
    fonts:
      nerd-fonts:
        - AdwaitaMono
  
  # Installs GNOME extensions from extensions.gnome.org
  - type: gnome-extensions
    install:
     - Tiling Assistant

  # Tests & compiles override files from gschema-overrides folder
  - type: gschema-overrides
    include:
      - zz1-startingleaf.gschema.override
      - zz1-tilingassistant.gschema.override
 
  # Installs and removes packages
  - type: dnf 
    repos:
      copr:
        - name: trixieua/morewaita-icon-theme
          chroot: fedora-43-x86_64
    install:
      packages:
        # Icon theme that adds icons for additional mimetypes and folders, as well as adwaita-style icons for third-party apps
        - morewaita-icon-theme
    remove:
      packages:
        # Unneeded / legacy GNOME apps removed
        - gnome-system-monitor
        - gnome-shell-extension-launch-new-instance
        - gnome-shell-extension-window-list
        - gnome-classic-session 
        - gnome-tweaks
        - gnome-software
